{
  "name": "socionics-typer",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/client",
    "build": "npm run build --workspace apps/client",
    "preview": "npm run preview --workspace apps/client",
    "deploy": "npm run deploy --workspace apps/client",
    "server": "npm run dev --workspace apps/server"
  }
}
